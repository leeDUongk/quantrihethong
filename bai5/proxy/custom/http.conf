# Ngu canh: http  (NPM tu include file nay vao khoi http cua nginx.conf)
#
# limit_req_zone BAT BUOC nam o ngu canh http, khong dat duoc trong tab Advanced
# cua Proxy Host vi tab do nam trong khoi server.
#
# DOI <MSSV> thanh ma so sinh vien cua minh truoc khi dung.
limit_req_zone $binary_remote_addr zone=login_MSSV:10m rate=5r/s;
